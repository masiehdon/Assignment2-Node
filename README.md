# Assignment2-Node

This repository contains a Node.js project that allows you to calculate the difference in days between a user-provided date and the current date. It also generates a runnable HTML file with the calculated date displayed.

## Features

- Calculates the difference in days between a user-provided date and the current date.
- Displays the calculated date in the command line interface.
- Generates a runnable HTML file.

